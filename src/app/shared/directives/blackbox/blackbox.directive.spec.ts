import { BlackboxDirective } from './blackbox.directive';

describe('BlackboxDirective', () => {
  it('should create an instance', () => {
    const directive = new BlackboxDirective();
    expect(directive).toBeTruthy();
  });
});
